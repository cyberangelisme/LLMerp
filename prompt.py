TPL = """
请为创建主题为{topic}的一个知识图谱，以简化JSON格式输出。格式要求：
1. 实体包含id、class、content和attach属性
2. 实体class可选值为：知识领域、知识单元、知识点、关键知识细节
3. 关系包含type、head和tail属性
4. 关系type可选值为：包含、次序
5. 不能有除json外的任何输出！！！！！！！！！！！

示例格式：
{{
  "entities": [
    {{"id": 1, "class": "知识领域", "content": "主题名称", "attach": "100000"}},
    {{"id": 2, "class": "知识单元", "content": "子主题1", "attach": "110000"}}
  ],
  "relations": [
    {{"type": "包含", "head": 1, "tail": 2}}
  ]
}}
请确保：
- 实体ID从1开始且唯一
- 关系的head和tail必须引用已定义的实体ID
- attach编码为6位数字
- 尽可能丰富内容，以及扩展深度，节点数不少于40个entities。
- 尽可能用多层结构（5-7层深度），一个节点上连接不超过4个子节点entities。根节点最好只有3个元素。
- 子节点和子节点不要交叉连接，多叉树结构最好不要有闭环回路。

请从以下内容获取知识：
{context}


"""


COMBINE_TEMPLATE = """
你已经收到了多个关于 "{topic}" 的文档片段中提取出的关键信息。
请将这些信息整合成一份连贯的、全面的概述，这份概述应包含文档的主要知识领域、知识单元、知识点及其之间的潜在关系。
目标是为构建一个知识图谱提供尽可能完整和准确的上下文。
原始提取信息:
{text}

整合后的知识概述:
"""



MAP_TEMPLATE = """
以下是关于 "{topic}" 的一个文档片段。请仔细阅读并提取其中可能成为知识图谱实体（知识领域、知识单元、知识点）和关系（包含、次序、关联等）的关键信息。
请以列表或概括性段落的形式列出这些信息，无需结构化为JSON。
文档片段:
"{text}"
提取信息:
"""